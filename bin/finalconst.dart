void main()
{
   final String name;
   name="ebin";
   //final can be declared and initialized in the name line or diffrent line but can be initializied only once
   const int age=20;// const should be initialized declered
   print(name);
   print(age);

}